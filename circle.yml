machine:
  services:
    - docker

dependencies:
  override:
    - pip install docker-compose
    - docker info
    - docker-compose build

test:
  override:
    - docker-compose up
    - docker-compose run web python manage.py syncdb
    - tox
