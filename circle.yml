machine:
  services:
    - docker

dependencies:
  override:
    - pip install 'docker-compose<1.3'
    - docker info
    - docker-compose build

test:
  override:
    - docker-compose run -d; sleep 20
    - docker-compose run web python manage.py syncdb
    - tox
